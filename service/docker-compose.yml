version: '3.8'
services:
  dauthenticator:
    image: gianksp/dauthenticator
    env_file:
      - .env
    build:
      context: ./
    volumes:
      - ./:/opt/app
    command: npm run start
    ports:
      - "4000:4000"
    container_name: dauthenticator-docker
    restart: always